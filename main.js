/**
 * TODO
 *
 * - add a banner explaning that it is a copy with no functionality
 *  - come up with text
 *  - come up with design
 *  - implement
 *
 * - create mobile view
 *  - design mobile view (do we need to have much difference, or just make what we have look nice)
 *      -header/footer/split view content pieces vertically
 *  - implement
 */

(function() {
  smoothScroll.addElement('image-clip-container', 0, 1, 2);
  smoothScroll.addElement('background-image2', 0, 1, 1.8);
  smoothScroll.addElement('background-image3', 50, -1, 1.9);
  smoothScroll.addElement('background-image4', -240, 1, 10);
  smoothScroll.addElement('background-image5', 65, -1, 1.2);
})();
